Added the ***cortex-content-1738*** tag, which binds to an identifier that enables GitLab runners to access Google resources. This tag replaces the need for manual authentication via the gcloud library.
